package st.ggviario.house.controller;

import javafx.scene.paint.Color;

public interface MaterialColors{
    Color PRIMARY_DARK = Color.valueOf( "#00796B" );
    Color PRIMARY = Color.valueOf( "#009688" );
    String NAME_PRYMARY ="#009688";
    Color ACCENT = Color.valueOf( "#FF5722" );
}
